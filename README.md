# VRA-Editor Releases

Built distributions of the [VRA Core 4 XML Editor](https://github.com/ztz88f5f6h-glitch/VRA-Editor) — the macOS `.app`, the Windows installer, and the Linux `.deb`.

This repo holds no source code; it exists only so releases can be downloaded without needing access to the private source repository. See the [Releases](../../releases) page for downloads.
